// chart-axis.js - 의존성 없음
console.log('[chart-axis.js] ✅ 로드됨 - 시간:', new Date().toISOString())

export function drawAxis(canvas) {
  console.log('[chart-axis.js] drawAxis() 호출됨')
  return {
    x: 0,
    y: 0,
  }
}
